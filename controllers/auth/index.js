const register = require('./register');
const login = require('./login');
const logout = require('./logout');
const current = require('./curent');
const updateAvatar = require('./updateAvatar')


module.exports = {
    register,
    login,
    logout,
    current,
    updateAvatar,
}